/* Config du site — modifiable si le site est déployé séparément de l'API.
   Par défaut, même origine (FastAPI sert le site + l'API). */
window.AIIS_CONFIG = {
  apiBase: "", // ex: "https://ton-serveur.onrender.com" en déploiement séparé
};
